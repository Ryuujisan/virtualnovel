trigger:
  branches:
    include:
      - master
  paths:
    include:
      - src/VirtualNovel.ImageService/**

pool:
  vmImage: ubuntu-latest

variables:
  imageRepository: virtualnovel-images
  dockerfilePath: src/VirtualNovel.ImageService/Dockerfile

steps:
  - checkout: self

  - task: Docker@2
    displayName: Build and push ImageService image
    inputs:
      command: buildAndPush
      containerRegistry: virtualnovel-acr
      repository: $(imageRepository)
      dockerfile: $(dockerfilePath)
      buildContext: $(Build.SourcesDirectory)
      tags: |
        $(Build.BuildId)
        latest
