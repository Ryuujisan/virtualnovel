trigger:
  branches:
    include:
      - master
  paths:
    include:
      - src/VirtualNovel.NovelService/**

pool:
  vmImage: ubuntu-latest

variables:
  imageRepository: virtualnovel-novels
  dockerfilePath: src/VirtualNovel.NovelService/Dockerfile

steps:
  - checkout: self

  - task: Docker@2
    displayName: Build and push NovelService image
    inputs:
      command: buildAndPush
      containerRegistry: virtualnovel-acr
      repository: $(imageRepository)
      dockerfile: $(dockerfilePath)
      buildContext: $(Build.SourcesDirectory)
      tags: |
        $(Build.BuildId)
        latest
