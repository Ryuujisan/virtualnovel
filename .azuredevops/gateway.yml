trigger:
  branches:
    include:
      - master
  paths:
    include:
      - src/VirtualNovel.Gateway/**

pool:
  vmImage: ubuntu-latest

variables:
  imageRepository: virtualnovel-gateway
  dockerfilePath: src/VirtualNovel.Gateway/Dockerfile

steps:
  - checkout: self

  - task: Docker@2
    displayName: Build and push Gateway image
    inputs:
      command: buildAndPush
      containerRegistry: virtualnovel-acr
      repository: $(imageRepository)
      dockerfile: $(dockerfilePath)
      buildContext: $(Build.SourcesDirectory)
      tags: |
        $(Build.BuildId)
        latest
